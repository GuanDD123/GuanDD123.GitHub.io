import random


def _():
    random.randbytes(n=int)  # 生成 int 个随机字节

    random.randrange(start=int, stop=int, step=int)
    random.randint(a=int, b=int)  # 返回 [int_1, int_2] 区间的随机一个整数

    random.choice(seq=list)  # 返回列表中随机一个元素
